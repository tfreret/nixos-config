# Copy this file to local.nix and fill in your values.
# local.nix is gitignored — never commit it.
{
  git = {
    personal = {
      name  = "Your Name";
      email = "you@personal.com";
    };
    work = {
      name  = "Your Name";
      email = "you@company.com"; # repos under ~/work/ will use this
    };
  };

  # ssh = {
  #   personal.key  = "~/.ssh/id_ed25519";
  #   personal.host = "github.com";
  #   work.key      = "~/.ssh/id_ed25519_work";
  #   work.host     = "gitlab.company.com";
  # };

  # gpg = {
  #   personal.signingKey  = "0xABCDEF1234567890";
  #   personal.signCommits = false;
  #   work.signingKey      = "0x1234567890ABCDEF";
  #   work.signCommits     = false;
  # };
}
